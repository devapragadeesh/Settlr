# CLAIMS

Every quantitative claim this repository makes, with the artefact that produces it, the command that reproduces it, its denominator and its scope.

**Generated by `corpus/claims_ledger.py` from the live run artefacts.** No value below is typed by hand. If a number here disagrees with a number elsewhere in the repository, this file is right and the other one has gone stale.

## Why this file exists, demonstrated on its first run

This table caught a live error **in itself, on its first execution**, in the file built to prevent exactly that class of error.

The first draft reported **14 abstentions on determined instances**, computed as `determined_instances − determined_resolved`. That subtraction is wrong. Those 14 lines are `AttestationDiscrepancy` — the resolver found a contradiction and reported a **finding**, which is not a silence — and the gate's abstention count is **0**. Writing the denominator and the scope down beside the number was what exposed it; nothing else in the repository had.

That is the argument for this artefact, and it is deliberately not the argument that the author was careful. **A mechanism caught what care did not.** The same conclusion `DECISIONS.md` §44.4 reaches about the reference-frame sweep, and `CHECKPOINT.md` §0 reaches about the leak audit: in this project, the informed searcher is not the control.

| claim | value | denominator | scope | produced by | reproduce with |
|---|---:|---|---|---|---|
| `Verified` assignments that are wrong | **0** | 275 `Verified` | 30 datasets, gate G1 | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| `ProvenUnmatched` rows that in fact settled | **0** | 701 proven rows | 30 datasets, gate G9 — the gate that did not exist before contract §4.7 | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| resolver enumerations completing at 48,566 rows | **0/12** | 12 CP-SAT solves, the largest `scale/data_*` fixture | runtime only, no accuracy claim -- `tests/test_scale_degradation.py` | `eval/resolver_scale_report.py` | `python3 eval/resolver_scale_report.py` (~26 min, resume-cached per point) |
| `Verified` that are non-decisive | **239** | 275 `Verified` | 30 datasets; a rival composition would have passed the same check | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| `Reconstructed` wrong | **0** | 1 `Reconstructed` | 30 datasets. **A COUNT, NOT A RATE** — the population is too small to support one | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| abstentions on determined instances | **0** | 164 determined instances | 30 datasets, gate G7 (attested). **An abstention is `Unresolved` or `Ambiguous` only.** The 14 determined instances that are not `Verified` are `AttestationDiscrepancy` — a *finding*, not a silence — and `instances − resolved` is therefore NOT the abstention count. The first draft of this table made that mistake | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| abstentions on reconstructible instances | **15** | 92 reconstructible instances | 30 datasets, gate G8. **Uniqueness is scoped to the pool the simulator drew from, 1.4×–14× smaller than the pool the resolver searches** — `DECISIONS.md` §46 | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| candidate sets not containing the truth | **20** | all candidate sets built, 30 datasets | gate G3 | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| datasets passing the oracle | **28** | 30 datasets | all nine gates; the 2 failures are both PSP-absence points, on G3 and G8 | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| `AttestationDiscrepancy` reported | **62** | 39 planted | 30 datasets. The excess is NOT a false-alarm rate — see the next two rows | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| `AttestationDiscrepancy` that are **genuinely false** | **0** | 62 reported | 30 datasets; each non-planted finding is checked against a `reversal_debit` line in the answer key | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| `AttestationDiscrepancy` that are true findings of a kind the corpus did not plant | **25** | 62 reported | 30 datasets; corroborated reversals | `corpus/oracle.py` | `python3 corpus/score_resolver.py --all` |
| `ProvenUnmatched` rows | **701** | 4308 `OpenBreak` rows alongside it (**deliberately not a shared denominator: one asserts, the other does not, and they are never summed** — `DECISIONS.md` §40) | 30 datasets; two entailed reasons only | `resolver/breaks.py` | `python3 corpus/score_resolver.py --all` |
| `OpenBreak` rows | **4308** | 701 `ProvenUnmatched` rows alongside it, never summed | 30 datasets. **Asserts nothing** and is never gated on correctness | `resolver/breaks.py` | `python3 corpus/score_resolver.py --all` |
| `OpenBreak` rows clustered under a causing bank line | **1582** | 4308 `OpenBreak` rows, 54 distinct causes | 30 datasets; `UPSTREAM_UNRESOLVED` only | `resolver/resolve.py:_blocked_by` | `python3 corpus/score_resolver.py --all` |
| `OpenBreak` rows the resolver could not classify | **1472** | 4308 `OpenBreak` rows | 30 datasets. A **real reported category**; a high count is an honest finding | `resolver/breaks.py` | `python3 corpus/score_resolver.py --all` |
| frozen cascade — datasets it can run on at all | **28** | 30 datasets | raises `KeyError: 'settlement_id'` at the PSP-absence points | `corpus/baseline_old_engine.py` | `python3 corpus/baseline_old_engine.py --all` |
| coverage on determinable lines — all 30 datasets | **276/297 (92.9%)** | 297 determinable settlement lines (359 total minus 62 whose record contradicts itself) | **A line the resolver MUST NOT answer is not a line it failed to answer.** The single-figure version counted them identically and so fell as detection improved (`DECISIONS.md` §48) | `corpus/coverage.py` | `python3 corpus/score_resolver.py --all` |
| coverage on determinable lines — the 28 datasets carrying a PSP settlement artefact | **275/275 (100.0%)** | 275 determinable settlement lines (335 total minus 60 whose record contradicts itself) | **A line the resolver MUST NOT answer is not a line it failed to answer.** The single-figure version counted them identically and so fell as detection improved (`DECISIONS.md` §48) | `corpus/coverage.py` | `python3 corpus/score_resolver.py --all` |
| coverage on determinable lines — the 2 PSP-absence datasets | **1/22 (4.5%)** | 22 determinable settlement lines (24 total minus 2 whose record contradicts itself) | **A line the resolver MUST NOT answer is not a line it failed to answer.** The single-figure version counted them identically and so fell as detection improved (`DECISIONS.md` §48) | `corpus/coverage.py` | `python3 corpus/score_resolver.py --all` |
| coverage on determinable lines — the original 14 -- the scope THREE_SYSTEMS.md publishes | **143/143 (100.0%)** | 143 determinable settlement lines (168 total minus 25 whose record contradicts itself) | **A line the resolver MUST NOT answer is not a line it failed to answer.** The single-figure version counted them identically and so fell as detection improved (`DECISIONS.md` §48) | `corpus/coverage.py` | `python3 corpus/score_resolver.py --all` |
| compositions a fifteen-line `GROUP BY` recovers | **322** | 335 attempted, over the 28 datasets it can run on | **on the other 2 it cannot run at all.** Highest resistance on a runnable dataset is 9.1% — one composition in twelve | `corpus/triviality_check.py` | `python3 corpus/triviality_check.py --all` |
| `CorrectlyUnmatched` accuracy — the SUPERSEDED outcome | **45.7%** | 4,994 claims over 30 datasets | **historical.** Split into `ProvenUnmatched` and `OpenBreak` by contract §4.7; 2,469 of those rows had settled | `investigation/DERIVED_BRANCH_AUDIT.md` | committed measurement, `investigation/DERIVED_BRANCH_AUDIT.md` |
| derived-branch reason accuracy — the SUPERSEDED outcome | **97.6%** | 1,872 claims | **historical, and the naive inference from it is wrong**: correcting the derivations raised rows-that-settled from 8 to 64, because a corrected `dispute_held` promotes rows out of a residual that asserts nothing into a branch that asserts something false (`DECISIONS.md` §40) | `investigation/DERIVED_BRANCH_AUDIT.md` | committed measurement, `investigation/DERIVED_BRANCH_AUDIT.md` |
| disputed rows whose `on_hold` column disagrees with their state at the horizon | **202** | 540 disputed rows | defect D13. Not fixable from this feed: the Razorpay dispute entity publishes no resolution timestamp (`DECISIONS.md` §44.5) | `investigation/DERIVED_BRANCH_AUDIT.md` | committed measurement, `investigation/DERIVED_BRANCH_AUDIT.md` |
| pool growth from the F1 fix | **+1.7% mean, +2.8% max** | all pools, 30 datasets; 1,544 row-slots added | `DECISIONS.md` §45; no gate moved | `investigation/F1_PREDICTION.md` | `python3 corpus/score_resolver.py --all` before and after `4b65764` |

## Claims with no generating artefact, or not measured at all

A number nobody can regenerate is a number nobody should trust, including whoever wrote it.

| claim | status |
|---|---|
| the ₹99,329.23 worked example in `README.md` | illustrative, drawn from Razorpay's published recon sample; it is not a measurement of this corpus and should not be read as one |
| `matching/` cascade metrics in `eval/EVAL_REPORT.md` | generated by `python3 eval/report.py --runs 3` against the FROZEN primary dataset, not against the corpus. Never comparable to any number above |
| closure count over the DERIVED pool at the 18 reconstructible instances | **MEASURED, but by a one-time enumeration rather than a pipeline step.** This row read "NOT MEASURED ... deliberately not built" until 2026-09-03, which was false: `investigation/D15_MEASUREMENT.md` took exactly this measurement and `SCORECARD.md` publishes its result. All **15/15** G8 abstentions are correct refusals -- at every one the resolver proved two or more closing subsets exist over the pool it can actually see, and at `A20_Bnone_Cmax` line 1 it proved so exhaustively (178 subsets, enumeration complete). It stays in THIS table because no command regenerates it: `corpus/scorecard.py` carries the figure as a held constant sourced from that document. Not reproducible is not the same as not measured, and this row conflated them |

## Two rules this table exists to enforce

1. **No rate without its denominator, inline.** Every value above carries one, including the ones where the denominator is embarrassing — `Reconstructed` wrong is a count over a population of one and says nothing about accuracy.
2. **`ProvenUnmatched` and `OpenBreak` are never summed.** One asserts, the other does not. The single place their total appears is as a denominator in this table, labelled as such (`DECISIONS.md` §40).
