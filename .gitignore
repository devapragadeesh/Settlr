__pycache__/
*.pyc
.venv/
.env
.razorpay/
graphify-out/

# scale/ fixtures are large and fully reproducible from generate_scale.py at
# the seed recorded in MANIFEST.json. The MEASUREMENTS are committed; the
# regenerable bulk is not.
scale/data_*/
scale/truth_*/
scale/DATASET_HASHES.txt

# generated by dashboard/build_dashboard.py -- the live store DB backing
# POST /runs/{run_id}/ask (DECISIONS Sec.101), rebuilt on every dashboard
# build, never hand-edited
dashboard/data/

# macOS
.DS_Store

# Local agent/tooling config and internal build notes -- kept on disk, never
# published. This is a product repo; how it was built is not part of it.
.claude/
CLAUDE.md
AGENTS.md
